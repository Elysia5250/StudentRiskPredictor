# 高校学生学业风险预测 — 课程报告素材

---

## 一、数据集介绍

本实验采用的数据集为CSV格式，共包含 **500** 条样本记录和 **10** 个字段。经自动分析，标签列为 **Grade**，特征字段共 **8** 个。

**数据集概览：**
- 样本总数：500
- 特征总数：8
- 标签字段：Grade
- 排除的ID字段：StudentID

## 二、数据预处理方法

本研究以 scikit-learn 为核心工具，预处理流程如下：

1. **标签列自动识别** — 通过关键词匹配和列特性分析自动推断。
2. **ID列过滤** — 自动排除学号、编号等无关字段。
3. **缺失值处理**：
   - 数值特征：中位数填充
   - 类别特征：众数填充
4. **类别特征编码** — One-Hot Encoding。
5. **数值特征标准化** — StandardScaler（零均值单位方差）。
6. **数据集划分** — 80% 训练 / 20% 测试（random_state=42，分层抽样）。

## 三、三种模型简介

### 3.1 逻辑回归 (Logistic Regression)

广义线性模型，通过 Sigmoid 函数将线性输出映射到 [0,1] 区间。可解释性强、计算高效，可输出各特征的权重系数。

### 3.2 决策树 (Decision Tree)

基于树结构的分类模型，递归选择最优特征划分数据，形成 if-then 规则。无需标准化、直观可解释，但易过拟合。

### 3.3 随机森林 (Random Forest)

基于 Bagging 的集成方法，构建多棵决策树并投票决策。抗过拟合、对缺失值鲁棒，可输出特征重要性排序。

## 四、模型评估结果分析

### 4.1 评估指标

- **准确率 (Accuracy)**：预测正确的样本占比。
- **精确率 (Precision)**：预测为正类中实际为正类的比例。
- **召回率 (Recall)**：实际为正类中被正确预测的比例。
- **F1 分数**：精确率与召回率的调和平均数。

### 4.2 评估结果

| 模型 | Accuracy | Precision | Recall | F1 |
|------|----------|-----------|--------|-----|
| Logistic Regression | 0.8300 | 0.8256 | 0.8300 | 0.8269 |
| Decision Tree | 0.9700 | 0.9729 | 0.9700 | 0.9704 |
| Random Forest | 1.0000 | 1.0000 | 1.0000 | 1.0000 |

### 4.3 结果分析

三个模型中 **Random Forest** 取得了最高的 F1 分数（1.0000），表明其在精确率和召回率之间取得了最佳平衡。

## 五、特征重要性分析

通过随机森林的 `feature_importances_` 评估各特征对预测的贡献程度。得分越高，预测能力越强。

**Top-5 重要特征：**

1. **ExamScore** — 重要性 0.6194
2. **AttendanceRate** — 重要性 0.1913
3. **HomeworkScore** — 重要性 0.0484
4. **PreviousGPA** — 重要性 0.0440
5. **StudyHoursPerWeek** — 重要性 0.0368

## 六、项目结论

本研究基于机器学习构建了学生学业风险预测模型，主要结论如下：

1. **模型有效性**：三种分类模型均在测试集上表现良好，其中 **Random Forest** 最优（F1 = 1.0000），验证了机器学习在学业风险预测中的可行性。
2. **关键影响因素**：特征重要性分析识别出了影响学生学业表现的关键因素，可为学校学业预警和教学干预提供参考。
3. **方法论价值**：本系统建立了完整的数据分析→建模→评估→可视化流程，可复现且易扩展。

---
*本报告由自动化分析系统生成 — 2026-06-19 13:55*